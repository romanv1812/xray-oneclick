# Xray One-Click (VLESS + REALITY) for Shadowrocket

This repo installs **Xray-core** and configures **VLESS + REALITY (XTLS Vision)**, then prints a ready-to-import `vless://` link for **Shadowrocket**.

## Supported OS
- Ubuntu / Debian (uses `apt`)

## One-liner usage

### Run from GitHub raw (branch)
Replace `USER/REPO/BRANCH`:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/romanv1812/xray-oneclick/main/install.sh)

